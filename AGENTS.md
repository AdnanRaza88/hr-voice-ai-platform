# Agent Registry Overview

This platform uses specialized agents with explicit contracts.

See `agents/registry.md` for the full registry and `agents/<id>/agent.md` for each agent contract.

Core agents (planned / in progress):
- Intent classifier
- Screening agent
- RAG / policy agent
- MOM agents (transcript, enhance, actions)
- TTS / STT voice layer
- Custom agent builder
